
<%= form_with(model: @brand, url: admin_brands_path, method: "post", class: "styled-form my-4") do |form| %>

  <% if @brand.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger">
        <h2><%= pluralize(@brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

        <ul>
          <% @brand.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group mb-3">
    <%= form.label :name, class: "form-label text-white" %>
    <%= form.text_field :name, class: "form-control rounded-0 bg-dark text-white" %>
  </div>

  <div class="text-start">
    <%= form.submit "Save", class: "btn btn-primary rounded-pill" %>
  </div>
<% end %>
