<h1>Brand Details</h1>

<p><strong>Name:</strong> <%= @brand.name %></p>

<%= link_to 'Edit', edit_admin_brand_path(@brand) %> |
<%= link_to 'Back', admin_brands_path %>
<%= button_to 'Delete', admin_brand_path(@brand), method: :delete, data: { confirm: 'Are you sure?' } %>

